public class prova {
    int num = 5, count = 1;
        while(count <= 3) {3
        ++count; 4
        num += count;10
    }
        System.out.println(num);

   // Dado o código acima, o que será impresso no console?
}
